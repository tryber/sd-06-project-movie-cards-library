import { render } from 'enzyme';
// implement MovieCard component here
import React from 'react';
class MovieCard React.Component {
  render () {
    const films = this.props.movies;
    return (
      <section>

      </section>
    );
  }
}

export default MovieCard;
